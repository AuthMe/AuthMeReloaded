# About the _tools_ Folder

This _tools_ folder provides helpers and extended tests useful during the development of AuthMe.
It now lives in the dedicated `authme-tools` module and does not contain unit tests.

Run the tools from the repository root with the `run-tools` Maven profile.
See the exact commands in [`docs/build.md`](../../../../../docs/build.md).
